[project]
name = "text-fitter"
version = "0.0.1"
description = "Dify plugin for smart text trimming to fit context windows"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "dify_plugin==0.7.4",
]
